{%- comment -%}
  品类文案的唯一来源:按商品 tag 取主题 locale 文案。
  不输出 Shopify 的 product.type —— 该字段不可翻译,原值混着日文与中文,会漏到另一语言的页面。
  无匹配品类时返回空,由调用方决定是否降级。
{%- endcomment -%}
{%- liquid
  if kind_product.tags contains '毛糸'
    echo 'yarn_project.card_badge_yarn' | t
  elsif kind_product.tags contains '編みものキット'
    echo 'yarn_project.card_badge_kit' | t
  elsif kind_product.tags contains '完成品'
    echo 'yarn_project.card_badge_finished' | t
  endif
-%}
