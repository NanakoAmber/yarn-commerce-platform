# Issue #23 内部原型验证证据

验证日期：2026-09-06。任务状态与验收勾选以 [Issue #23](https://github.com/NanakoAmber/yarn-commerce-platform/issues/23) / PR 为准；本文记录可复核的操作和结果，不替代任务状态。

## 当前返工：作品优先、部分配件销售

本节为最新结果。下文初版记录保留历史证据，其中英文未发布、邮件待授权和旧 finish verdict 不再代表当前状态。

| 验证 | 当前结果 |
| --- | --- |
| 四项清单 | 小袋 `demo-granny-pouch` 关联 `demo-pouch-yarn`、`demo-pouch-hook`、`demo-pouch-finishing`、`demo-pouch-strap`；分别为本店提供、自备、自备、待补。规格、用途、说明与采购数量来自后台，不把部分供给称为齐套 |
| 真实商品规格 | 第一项关联 Joyful 02 / `54418006901049`，前台读取 ¥2,200；替换 Puni 03 / `54418006049081` 后显示 ¥780 和新商品入口，再恢复。没有复制或改动运营价格、库存 |
| 修改供给 | 本店提供改自备保存后，该项按钮数为 0；作品标题、其他工具与教程仍在。恢复本店提供后入口恢复 |
| 状态切换 | 配件 Active 改 Draft 后仅该项消失；Project 和教程继续展示。恢复 Active。未为测试缺货而修改运营库存；缺货分支由 Liquid 与自动检查覆盖 |
| 新建与排序 | 新建四条配件并关联作品；交换钩针与收尾工具后前台顺序同步；恢复线材、钩针、收尾工具、可选挂绳。未删除旧 materials / tools 数据 |
| 三语内容 | 四条配件的 Title、Specification、Note 均已维护并前台核对中英日；Supply 枚举、Variant 引用、数量不翻译。英文已获用户当次授权开放，`/en` 不再是未发布缺口 |
| 逐项购买 | 中文、英文在作品页直接加购，只有被选择的一项加入；英文购物车数量 1→2 成功。无工具自动加入 |
| 商品来源 | 三语原始商品入口携带作品和材料。日文商品页 02→03 切换后来源参数保留，实际加购得到 03 且 Project / Component 正确；中文商品页加购同样保留来源 |
| 教程连续性 | 中英日“开始制作”定位教程；教程中的材料链接返回同一 `ProjectPrepare`。不购买也可阅读 |
| 成品连续性与兼容 | 英文首页毯子卡→同名详情→成品商品→加购；商品页与购物车均保留作品回链及非教程同款说明。该作品未迁移新清单，旧字段仍可显示无材料关联而保留教程 |
| 普通商品访问 | 不带来源参数打开同一成品商品，不显示作品来源；不建立跨页全局或账号状态。URL 来源仅为可修改导航提示，不作为适配、身份认证或商业真值 |
| 清理 | 仅移除本轮加入的所有 Joyful 和成品行，最后保留原有 Puni 01 / `54418005983545` 数量 1，总额 ¥780 |
| 安全 | 未发布 Theme、未下单支付、未发 Slack。用户取消本轮测试邮件，咨询发送保持关闭，实际收件未验证，不再等待邮件授权 |

当前 `npm run verify`：50/50 pass；Theme Check 0 errors / 12 条原有 warnings；`git diff --check` pass。新测试覆盖逐项 POST、数量约束、重复提交、确定性失败可重试、不确定结果先查购物车、返回上下文、无效/跨站来源过滤与不覆盖 Variant 商业事实。适用 detect：配件 UI 15 advisory（继承的字号、颜色、圆角）；后续新增商品来源 UI 再检查为 3 条既有库存状态色 advisory，无阻断。该后续检查针对新增文件，不是重复无变化扫描。

本版截图在本地 `.impeccable/review/23-project-components/`，三语各 1440 / 390 作品全页；商品来源、非同款成品与购物车补充视口；恢复浏览器后实际 819 CSS px 也打开检查，无水平溢出。完整截图清单和行为边界见 [复核输入](../../.impeccable/review/23-project-components/review-input.md)。含未核实公开素材权利的运营商品照片，截图不进入公开 diff。

本版独立 [finish review](../../.impeccable/review/23-project-components/finish-review.md) 提出两项 `fix`：英日手机首屏准备区过低，以及 DESIGN 事实同步。移动排布已集中调整为完整封面与标题并排，全文简介、事实和披露保留；精确 390×844 初始视口中，准备标题 / 正文 top 为英文 647 / 698、日文 602 / 653、中文 523 / 574，均在 CTA top 761 上方，无横向溢出。三语首屏和全页已重拍并打开；独立 documenter 已合并同步 DESIGN 与 sidecar。同一评审的 [最终 verdict](../../.impeccable/review/23-project-components/finish-verdict.md) 将本轮两项修复均评为 `resolved`、`disposition: ship`，只覆盖这两项修复，不冒充全站或生产批准。初版两项 `resolved` 不代替本版复核。

产品标题和部分既有 Theme Editor 运营文案仍为日文；三语关键操作可用不等于整站内容已完全本地化。材料适配、实际用量、跟做结果、公开素材权利、供货与队友试用仍需人工确认。

## 初版记录（返工前）

## 预览与隔离

- 未发布 Theme：`189727637817`，名称 `Issue 23 - Project prototype`。
- [受保护预览](https://tutaka-54.myshopify.com/?preview_theme_id=189727637817)；需要当前店铺密码或已授权会话。跨页面以预览会话保持主题，不以最终 URL 是否保留参数判断主题。
- 独立未登录浏览器停在 `/zh/password`；独立 HTTP 访问同样被重定向至密码页。没有移除密码、发布主题或修改原有线上主题。
- 3 条新 `yarn_project` 使用 `仅内部原型` 范围。只有本未发布 Theme 打开内部作品开关。Active 是 Shopify 读取门槛，不是业务公开批准，也不是内容保密机制。

## 已实际执行

| 路径 | 操作及结果 |
| --- | --- |
| 首页 → 作品详情 | 新建的方形收纳篮、小袋、盖毯均可通过作品库进入统一 Metaobject 页面；精选在 Theme Editor 选择篮子和小袋，不复制作品内容 |
| 搜索与组合筛选 | `花片` + `配饰` 为小袋 1 条；再选 `新手` 为 0 条，显示无结果；清除恢复 3 条，不把待核实难度归为新手 |
| 手机更多筛选 | 390 CSS px 展开额外条件，`5/0` 返回小袋 1 条；无页面水平溢出 |
| 同页教程 | 桌面盖毯、手机小袋的开始制作定位 `#ProjectTutorial` 并移交焦点；小袋两个 H3 形成独立可展开步骤，来源明确跳转原作者网站 |
| 材料购买（桌面） | 小袋 → Joyful Cake Yarn → 色号 `02` / Variant `54418006901049` → 加购 1 → 改为 2 → Shop Pay 结账入口。未填写、截图或提交结账信息，未下单支付 |
| 成品购买（桌面） | 盖毯 → 关联花束毯成品 → `レッド` / Variant `54418017747257` → 加购 1 → 改为 2 → Shop Pay 结账入口。成品与教程不同设计，页面明确标为参考 |
| 材料购买（手机） | 390 CSS px 选择上述材料真实色号、加购、购物车数量增加成功；购物车布局单独交 finish review，不把按钮能点击当成视觉通过 |
| 购物车恢复 | 每次只删除本次自行添加的材料 / 成品行；原有 Puni 色号 `01`、数量 1 保持不变 |
| 后台名称编辑 | Entry `279382294841` 从“花片小袋｜内部示例”改为“花片双拼小袋｜内部示例”，首页两处卡片及详情同步，Handle 不变 |
| 首页排序 | 用 Theme Editor 原生键盘排序把小袋移到第一位，保存后前台精选顺序同步；随后恢复篮子在先、小袋在后的原顺序 |
| 后台教程编辑 | 使用 Shopify 原生富文本 H3 与段落，补充“准备好自己的工具后再开始；工具不会自动加购。”；保存后前台第二步展开显示完整新增正文 |
| 后台商品关联编辑 | 暂时移除小袋材料引用并保存，前台不再出现 Joyful，显示无关联材料；恢复同一真实 Product 引用后商品入口恢复。未修改 Product 价格、库存或发布状态 |
| 独立商品编辑 | 仅新建 Draft Product `10331691090233`，所有销售渠道均取消、库存 0；在指定 Theme `189727637817` 原生 PDP 验证两次标题与描述修改，最终恢复“作品原型编辑验证｜内部草稿”。未触碰既有运营 Product |
| 已发布语言 | 从真实语言切换入口验证日文根路径 `/`（`html lang=ja`）及中文 `/zh`；商品页日文加入购物车控件正常。3 条样例的日文叙述字段已保存；小袋前台标题、摘要、制作准备、两节教程及来源均显示日文 |
| 日文发现与双端 | `ポーチ` + `小物・アクセサリー` 为 1 条，再选 `はじめて` 为 0，清除恢复；390 px 同页教程定位并聚焦 `ProjectTutorial`。1440 / 390 CSS px 无页面水平溢出，标题与摘要没有截断 |
| 资源翻译 | Translate & Adapt 中 3 条作品的日文 / 英文各 5 个叙述字段重载后有值；小袋保留 2 个 H3，篮子 1 个 H3。未翻译稳定枚举、Hook size 或 URL。英文只保存内容，未发布语言 |
| 菜单与首页译文 | 专用菜单 4 项按中文原文逐行核对日英译文，菜单标题翻译为空；日文桌面与手机菜单的标签和实际链接匹配。仅 Theme `189727637817` 的首页精选标题新增日英翻译，英文重载有值、日文前台可见 |

后台保存后 Shopify 展示存在短暂传播延迟；检查最终保存状态并重新加载确认，不通过修改模板来“修复”尚未传播的数据。Draft Product 官方预览 key 是内容快照，编辑后须从 Product 页面重新点预览生成新 key；带预览 key 的 URL 不写入公开仓库。新 Draft 创建时价格留空，Shopify 保存后显示默认 Variant `¥0`；这是不可售演示草稿，不是免费商品或真实报价。

## 自动检查与视觉证据

- `npm run verify` 最终通过：39 个测试覆盖筛选、分页完整性与失败恢复、locale 路径、真实商品内容来源、菜单来源、表单安全接线和手机布局规则；`git diff --check` 通过。静态测试不替代真实收件或商品编辑验证。
- Theme Check 当前 0 errors / 12 warnings：11 条原有基线，加上新导航接线后旧 `header-dropdown-menu.liquid` 未引用警告。保留旧 snippet 以减小回滚范围。
- 适用 detector 0 阻断项、13 条 advisory：作品样式 10 条字号 / 颜色 / 圆角与既有 DESIGN 的差异，商品页 3 条原有库存状态颜色。交由 finish review 判断，不以更新规范掩盖失败。
- 独立 [finish review](../../.impeccable/review/23-project-detail/finish-review.md) 初次给出 `fix`：手机详情首屏制作准备不可见、手机数量步进器破损。集中修改、实拍并交回同一评审后，[verdict](../../.impeccable/review/23-project-detail/finish-verdict.md) 将两项均记为 `resolved`，`disposition: ship` 仅针对这两项修复，不代表英文、咨询或整体 Issue 通过。评审通过独立 Sol 代理加载仓库角色定义执行；没有伪造已配置的 agent type 或旧 build gate。
- 双端截图保存在 `.impeccable/review/23-project-detail/`，逐张打开检查；`*-final.png` 为最终复核输入。Chrome 缩放下先核对实际 `innerWidth` 为 1440 / 390；截图排除滚动条的位图宽度为 1431 / 381。
- `home-zh-desktop-1440.png` 是失真的旧 full-page 捕获，不作证据；使用 `home-zh-desktop-1440-final.png`。旧手机购物车图只作修改前证据。
- 补充打开检查 `project-ja-desktop-1440-final.png` 与 `project-ja-mobile-390-final.png`：导航修正后标签与目标相符，日文长文完整换行；手机日文制作准备位于首屏下沿之后（实测约 y=803），底部制作按钮可用。这是语言扩展后的已知布局差异，不把此前中文首屏 verdict 扩大成日文首屏完全一致的结论。本轮未继续像素级调整。
- 仓库为公开仓库。含现有店铺商品照片的浏览器截图只留本地受控审查，不上传未核实版权的素材；结账页面及后台私人信息不进入截图提交。

## 初版尚未通过项（当前状态以上方返工节为准）

- 初版英文尚未发布；后续已获用户授权开放并完成本轮关键路径验证，详见上方。
- Shopify 联系表单接收目标已在后台核对；后续用户取消测试邮件。实际收件、错误 / 成功页面仍未实测，发送开关保持关闭；接收地址不写入公开仓库。
- 首页选取、重新排序与 Draft 商品编辑已实测；队友亲手照指南操作、真实跟做、材料适配、供货和素材公开使用权均另列人工验收。
- 不发布、不下单、不发送 Slack，不因内部演示技术通过而承诺正式可售或教程能够跟做。

## 回滚

关闭未发布 Theme 的内部作品开关、取消新首页 Section / 恢复之前首页与菜单配置，或将该未发布 Theme 回退到任务前代码。演示菜单仅绑定该 Theme。共享 Metaobject 定义和样例不会随 Git 回退消失；不得删除运营数据来回滚 UI，删除新共享记录也应先单独确认。完整入口与字段见 [内容模型](../project-content-model.md) 和 [后台操作指南](../project-prototype-operator-guide.md)。
